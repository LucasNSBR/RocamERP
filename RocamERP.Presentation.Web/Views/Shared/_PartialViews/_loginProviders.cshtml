@foreach (var provider in HttpContext.Current.GetOwinContext().Authentication.GetAuthenticationTypes())
{
    if (provider.Caption != null)
    {
        @Html.ActionLink(provider.Caption, "ExternalLogin", new { @provider = provider.Caption, @returnUrl = ViewBag.returnUrl }, new { @class = "btn btn-primary" })
    }
}
