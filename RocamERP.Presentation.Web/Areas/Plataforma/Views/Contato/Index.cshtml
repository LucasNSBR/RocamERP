@model IEnumerable<RocamERP.Presentation.Web.ViewModels.ContatoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Observacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClienteId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Observacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClienteId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.RegistroEstadual)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.RegistroFederal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContatoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContatoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContatoId })
        </td>
    </tr>
}

</table>
