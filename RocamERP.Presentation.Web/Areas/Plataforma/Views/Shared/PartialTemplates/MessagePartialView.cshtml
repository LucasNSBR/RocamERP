@using RocamERP.Presentation.Web.Messager

@if (TempData["Message"] != null)
{
    BaseMessage message = (BaseMessage)TempData["message"];
    @Html.MessageAlert(message.Title, message.MessageType);
}
